import React from "react";
import './Loading.css';

const Loading = () => {
    return (

        // Componente de carregamento
        <div className="loading">
        </div>
        
    );
};

export default Loading;